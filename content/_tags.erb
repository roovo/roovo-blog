---
filter:   erb
---

<% sidebar_box "Tags" do %>
  <ul>
    <% tags_hash.keys.sort.each do |tag| %>
      <li><%= link_to(tag, "/tags/#{tag}.html") << " (#{tags_hash[tag]})" %></li>
    <% end %>
  </ul>
<% end %>
